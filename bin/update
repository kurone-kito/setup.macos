#!/bin/sh
# vim: set ft=sh :

set -eu

brew upgrade
brew cleanup

vagrant plugin update

./update_docker

git -C "${ZDOTDIR:-$HOME}/.zprezto" pull
asdf plugin update --all
mas upgrade
./update_system
