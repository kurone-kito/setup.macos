#!/bin/sh
# -*- mode: sh -*-
# vim: set ft=sh :

export EDITOR='vim'
export LANG='ja_JP.UTF-8'
# LC_ALL is deliberately not set: it outranks every other locale variable,
# including LANG above, so a later per-category override (e.g. LC_COLLATE=C
# for one command) would be silently ignored while it is exported globally
# here. See #119.
